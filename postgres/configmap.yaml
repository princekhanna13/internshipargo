apiVersion: v1
kind: ConfigMap
metadata:
  name: data-config
data:

  db_url: postgres://postgres:password@https://kubernetes.default.svc:5435/postgres?sslmode=disable

  db_pass: password
  db_name: test
  db_user: postgres
  mail: admin@atoss.com
  password: adminpassword