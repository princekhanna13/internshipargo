apiVersion: apps/v1
kind: Deployment
metadata:
  name: internshipnode
  labels:
    app: internshipnode
spec:
  replicas: 1
  selector:
    matchLabels:
      app: internshipnode
  template:
    metadata:
      labels:
        app: internshipnode
    spec:
      containers:
        - name: node
          image: bumbaruflaviusgabriel/internshipnode:sha-c838744
          resources:
            limits:
              memory: "250Mi"
              cpu: "250m"
            requests:
              memory: "150Mi"
              cpu: "100m"
          ports:
            - containerPort: 8000
          env:
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  key: redis_url
                  name: redis-configmap
            - name: KEYCLOAK_HOST
              value: keycloak-service.argokeycloakyaml.svc.cluster.local
